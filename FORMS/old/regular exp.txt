PROCEDURE CHECK_VALIDATE_ALPHABATIC(X IN VARCHAR2) IS

	B number(15):=0;
BEGIN 
IF X IS NOT NULL THEN	  
--	message('start validate alphabatic');
  select count(*) into B from DUAL where regexp_like(x,'^[a-zA-Z ]*$');
	
	IF B=0 THEN
		MESSAGE('Feild is not alphabatic, kindly check the feild.');
		RAISE FORM_TRIGGER_FAILURE;
	END IF;
--	message('end validate alphabatic');
END IF;
END;